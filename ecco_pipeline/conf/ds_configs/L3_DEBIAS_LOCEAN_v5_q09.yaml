ds_name: L3_DEBIAS_LOCEAN_v5_q09
start: 19800101T00:00:01Z # yyyymmddThh:mm:ssZ
end: NOW # yyyymmddThh:mm:ssZ
date_regex: "%Y-%m-%dT%H:%M:%SZ" # does not change

# Provider specifications
harvester_type: ifremer_ftp
host: ftp.ifremer.fr
user: ext-catds-cecos-locean # does not change
password: catds2010
ddir: Ocean_products/L3_DEBIAS_LOCEAN/L3_DEBIAS_LOCEAN_v5/debiasedSSS_09days_v5/
regex: \d{8}
filename_filter: "" # string to match for filenames to download ex: polstere-100_multi

# Metadata
aggregated: false # if data is available aggregated
data_time_scale: "daily" # daily or monthly
date_format: "yyyymmdd" # format of date in file name ex: yyyymmdd
regions: [] # regions data is split into ex: ['north', 'south']
fields:
  - name: "SSS"
    long_name: "Unbiased Sea Surface Salinity"
    standard_name: "sea_surface_salinity"
    units: "pss"
  - name: "eSSS"
    long_name: "Sea Surface Salinity error"
    standard_name: "standard_error_sea_surface_salinity"
    units: "pss"
original_dataset_title: "SMOS SSS L3 maps generated by CATDS CEC LOCEAN. debias V5.0"
original_dataset_short_name: "L3_DEBIAS_LOCEAN_v5_q09"
original_dataset_url: "https://www.catds.fr/Products/Available-products-from-CEC-OS/CEC-Locean-L3-Debiased-v5"
original_dataset_reference: "https://www.seanoe.org/data/00417/52804/#79565"
original_dataset_doi: "10.17882/52804"

# Transformation
t_version: 1.0 # Update this value if any changes are made to this file
array_precision: float32
time_zone_included_with_time: False # Data time values have time zone
extra_information: ["time_bounds_var"] # Unique things to do for dataset
data_res: 25/111 # Resolution of dataset

# Values for non split datasets (for datasets split into nh/sh, append '_nh'/'_sh')
data_max_lat: 86 # Maximum latitude in dataset
area_extent: [-17367530.44, 7307375.92, 17367530.44, -7307375.92] # [lon_min, lat_min, lon_max, lat_max]
dims: [1388, 584] # [lon, lat]
proj_info:
  area_id: cea
  area_name: Equal Area Cylindrical
  proj_id: EPSG:6933
  proj4_args: +proj=cea +lon_0=0 +lat_ts=30 +x_0=0 +y_0=0 +ellps=WGS84 +towgs84=0,0,0,0,0,0,0 +units=m +no_defs

pre_transformation_steps: [] # List of functions to call on the DataSet before transformation
post_transformation_steps: [] # List of functions to call on the DataArrays after transformation
notes: "" # String to include in global attributes of output files

# Aggregation
a_version: 1.0
remove_nan_days_from_data: True # Remove empty days from data when aggregating
do_monthly_aggregation: True
skipna_in_mean: True # Controls skipna when calculating monthly mean
monthly_tolerance: 8 # Defines the range of days to look for data around the start of the month (exclusive)
save_binary: True
save_netcdf: True
